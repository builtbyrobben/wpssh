version: "2"

linters:
  default: none
  enable:
    - asciicheck
    - bidichk
    - bodyclose
    - containedctx
    - contextcheck
    - cyclop
    - depguard
    - dupword
    - dupl
    - err113
    - errcheck
    - errorlint
    - forbidigo
    - goconst
    - gocritic
    - gocyclo
    - gosec
    - govet
    - ineffassign
    - makezero
    - misspell
    - nilerr
    - nilnil
    - noctx
    - nolintlint
    - paralleltest
    - prealloc
    - predeclared
    - revive
    - rowserrcheck
    - sqlclosecheck
    - staticcheck
    - tagliatelle
    - thelper
    - tparallel
    - durationcheck
    - unparam
    - unused
    - wastedassign
    - whitespace
    - wrapcheck
    - wsl_v5

  settings:
    cyclop:
      max-complexity: 50
    dupl:
      threshold: 150
    depguard:
      rules:
        main:
          list-mode: lax
          files:
            - $all
          deny:
            - pkg: io/ioutil
              desc: "deprecated; use io or os"
    goconst:
      min-len: 3
      min-occurrences: 4
    gocyclo:
      min-complexity: 50
    forbidigo:
      forbid:
        - pattern: "^log\\."
          msg: "avoid std log; use slog"
    revive:
      rules:
        - name: blank-imports
        - name: empty-lines
        - name: errorf
        - name: import-shadowing
        - name: range
        - name: unnecessary-stmt
    tagliatelle:
      case:
        rules:
          json: snake
          yaml: snake
    wsl_v5:
      allow-first-in-block: true
      allow-whole-block: false
      branch-max-lines: 2
      disable:
        - assign
        - decl
    govet:
      enable-all: true
      disable:
        - fieldalignment

  exclusions:
    presets:
      - std-error-handling
    rules:
      - path: _test\.go
        linters:
          - errcheck
          - cyclop
          - gocyclo
          - gosec
          - dupl
          - goconst
          - tagliatelle
          - paralleltest
          - err113
          - errorlint
          - wsl_v5
          - noctx
          - unparam
      - path: internal/cmd/.*\.go
        linters:
          - err113
          - wrapcheck
          - dupl
          - wsl_v5
          - tagliatelle
          - gocritic
          - gosec
          - govet
          - nilerr
          - predeclared
          - staticcheck
          - unparam
      - path: cmd/.*\.go
        linters:
          - wsl_v5
      - path: internal/(adapter|batch|cache|config|outfmt|registry|safety|scripts|ssh|wpcli)/.*\.go
        linters:
          - err113
          - wrapcheck
          - wsl_v5
          - noctx
          - gosec
          - nilerr
          - nilnil
          - govet
          - errorlint
          - unparam
          - tagliatelle
          - gocritic
          - predeclared
          - staticcheck
          - revive
          - rowserrcheck
      - linters:
          - errcheck
        text: "Error return value of `fmt\\.Fprint"
      - linters:
          - errcheck
        text: "Error return value of .*.Close"
      - linters:
          - errcheck
        text: "Error return value of .*.Signal"

formatters:
  enable:
    - gofumpt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/builtbyrobben/wpssh
